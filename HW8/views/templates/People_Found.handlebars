{{#if hasPeople}}
<h1> People Found </h1>
<h2> Supplied Name: {{personName}} </h2>
    <ul> 
        {{#each person}}
            <li> <a href = "/details/{{id}}"> {{name}} </a> </li>
        {{/each}}
    </ul>
{{else}}
<p class="not-found"> We're sorry, but no results were found for {{personName}}.</p>
{{/if}}

