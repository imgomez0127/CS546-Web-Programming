<div class="content">
{{#if hasPeople}}
<div>
<h1 class="title"> People Found </h1>
<h2 class="suppliedName"> Supplied Name: {{personName}} </h2>
    <div>
        <ul> 
            {{#each person}}
                <li> <a href = "/details/{{id}}" class="name"> {{name}} </a> </li>
            {{/each}}
        </ul>
    </div>
</div>
{{else}}
<p class="not-found"> We're sorry, but no results were found for {{personName}}.</p>
{{/if}}
</div>
